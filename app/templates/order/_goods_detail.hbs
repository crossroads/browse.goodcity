<section class="main-section your-detail client-information large-10 large-offset-1 medium-10 medium-offset-1 small-12">
  <div class="row">
    <div class="small-12 columns type-of-id-text">
      <h1 class="title"> {{t "order.confirm_booking.goods_detail.title"}} </h1>
    </div>
  </div>

  {{#each order.goodcityRequests as |request|}}
    <div class="row info">
      <div class="small-4 columns">
        {{request.packageType.name}}
      </div>
      <div class="small-5 detail columns">
        {{request.description}}
      </div>
      <div class="small-2 detail columns">
        {{request.quantity}}
      </div>
    </div>
  {{/each}}
</section>
