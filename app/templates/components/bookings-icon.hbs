<div class="notifications">
  <i class="fa dashboard-menu item-menu">
    {{fa-icon 'calendar-alt' prefix="far" }}
  </i>
  {{#if hasMessages}}
  <span class="notifications unread tab-unread-count">
    <div class="unread_length">{{unreadBookingsMessagesCount}}</div>
  </span>
  {{/if}}
</div>
Bookings
